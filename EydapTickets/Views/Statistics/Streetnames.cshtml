
@model EydapTickets.Models.StatisticsCreteriaModel

@Html.DevExpress().ComboBox(
        settings =>
        {
            settings.Name = "StreetNameStatistics";
            settings.CallbackRouteValues = new { Controller = "Statistics", Action = "Streetnames" };
            settings.Width = Unit.Percentage(100);
            settings.SelectedIndex = 0;

            settings.Properties.IncrementalFilteringMode = IncrementalFilteringMode.StartsWith;
            settings.Properties.DropDownStyle = DropDownStyle.DropDown;

            settings.Properties.TextField = "Text";
            settings.Properties.ValueField = "Value";

            settings.Properties.CallbackPageSize = 100;

            settings.Properties.ClientSideEvents.BeginCallback = "function(s, e) { e.customArgs['Municipality'] = MunicipalityStatistics.GetValue(); }";
        }
    ).BindList(Model.StreetNames).Bind(Model.StreetNameStatistics).GetHtml()
